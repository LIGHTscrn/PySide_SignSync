[project]
name = "major-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LIGHTscrn", email = "pranavprasad.r500@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.11.2",
]

[project.scripts]
major-project = "major_project:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
